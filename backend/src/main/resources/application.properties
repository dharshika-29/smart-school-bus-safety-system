# ---------- Server ----------
server.port=8080

# ---------- MySQL ----------
# Local MySQL-la "bus_safety" nu database create pannunga: CREATE DATABASE bus_safety;
spring.datasource.url=jdbc:mysql://localhost:3306/bus_safety?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=dharshi@29
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------- JPA / Hibernate ----------
# update = tables illana create pannum, irundha alter pannum (development-ku convenient)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ---------- JWT ----------
# Production-la idha vera oru long random string-a maathunga (min 32 characters)
app.jwt.secret=change_this_device_secret_key54-n
app.jwt.expiration-ms=604800000

# ---------- Device key ----------
# Bus GPS device / driver app idha "x-device-key" header-la anuppanum
app.device.key=change_this_device_secret_keytuil

# ---------- CORS ----------
# React dev server URL. Production-la unga deployed frontend URL add pannunga.
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000
